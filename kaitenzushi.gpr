project Kaitenzushi is
    for Main use ("main.adb");
    for Source_Dirs use ("src", ".");
    for Object_Dir use "src/obj";
    for Exec_Dir use "exec";

   package Compiler is
    for Default_Switches ("Ada") use ("-g");
    end Compiler;
end Kaitenzushi;
